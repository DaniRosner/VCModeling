{
  "scripts": {
    "start": "python3 -m http.server 4174 --bind 127.0.0.1",
    "test": "node tests/model.test.js",
    "deploy": "npx wrangler pages deploy . --project-name mv-portfolio-model"
  },
  "devDependencies": {}
}
